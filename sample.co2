int main() {
    int a;
    a = 5;

    // Comment
    int x;
    
    x = 5;

    (((x))) += 5;

    int b = a * 5 + 10;

    int z = (0x10Fa + x * (-10));

    z %= 64 * 10  * (z * 7);

    int too_big_hex = 0xFFFFFAFF;
    int too_big_dec = 999999;

    return 5+7*z;
}